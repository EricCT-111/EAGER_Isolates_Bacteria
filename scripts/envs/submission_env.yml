name: submission_env
channels: [conda-forge, bioconda]
dependencies:
  - python=3.11
  - table2asn # creates .sqn for genome submission
  - biopython # record editing
  - seqkit
  - csvtk # discrepancy-report and TSV wrangling
  - gffread # GFF/GTF formatting
